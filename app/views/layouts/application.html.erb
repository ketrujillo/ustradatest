<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Ustrada</title>
  <% if current_landlord.present? %>
    <link href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.4/cerulean/bootstrap.min.css" rel="stylesheet">
  <% elsif current_renter.present? %>
    <link href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.4/united/bootstrap.min.css" rel="stylesheet">

  <% else %>

    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
  <% end %>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">

  <style>
    /* Only necessary if you are using navbar-fixed-top */
    body {
      padding-top: 60px;
    }
  </style>

  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

  <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <% if current_landlord.present? %>
                <a href="/"><img src="http://i.imgur.com/EqCRk31.gif" alt="Ustrada" class="img-responsive" width="85" height="52"></a>
        <% elsif current_renter.present? %>
                <a href="/"><img src="http://i.imgur.com/O7DLRYE.gif" alt="Ustrada" class="img-responsive" width="85" height="52"></a>
        <% else %>
        <a href="/" class="navbar-brand">Ustrada</a>
        <% end %>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
        <% if current_landlord.present? %>
          <li><a href="/listings">Listings</a></li>

        <% elsif current_renter.present? %>
          <li><a href="/listings">Listings</a></li>
          <li><a href="/applications">My Applications </a></li>

        <% else %>

        <% end %>


        </ul>

        <ul class="nav navbar-nav navbar-right">
          <% if current_landlord.present? %>
            <li><a href="/landlords/edit"><%= current_landlord.email %></a></li>
            <li><a href="/landlords/sign_out" data-method="delete" rel="nofollow">Sign out</a></li>
          <% elsif current_renter.present? %>
            <li><a href="/renters/edit"><%= current_renter.email %></a></li>
            <li><a href="/renters/sign_out" data-method="delete">Sign out</a></li>

          <% else %>
            <li><a href="/renters/sign_in">Renter</a></li>
            <li><a href="/landlords/sign_in">Landlord</a></li>
          <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div>
  </nav>

  <div class="container">
    <% if notice.present? %>
      <div class="alert alert-dismissable alert-success">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <%= notice %>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="alert alert-dismissable alert-warning">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <%= alert %>
      </div>
    <% end %>

    <%= yield %>
  </div>
</body>
</html>
